<%= simple_form_for(@coffee_shop) do |f| %>
  <%= f.error_notification %>

  <%= f.input :name %>
  <%= f.input :district, collection: [] %>
  <%= f.input :state, collection: [] %>

  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>
